diff --git a/node_modules/cozy-pouch-link/dist/db/sqlite/sql.js b/node_modules/cozy-pouch-link/dist/db/sqlite/sql.js
index 6cbc128..a39d0ac 100644
--- a/node_modules/cozy-pouch-link/dist/db/sqlite/sql.js
+++ b/node_modules/cozy-pouch-link/dist/db/sqlite/sql.js
@@ -247,7 +247,7 @@ var makeSQLQueryFromMango = function makeSQLQueryFromMango(_ref2) {
       skip = _ref2$skip === void 0 ? 0 : _ref2$skip;
   var whereClause = makeWhereClause(selector);
   var sortClause = makeSortClause(sort);
-  var sql = ["SELECT json AS data, doc_id, rev", "FROM 'by-sequence' INDEXED BY ".concat(indexName), "WHERE ".concat(whereClause)].join(' ');
+  var sql = ["SELECT 'by-sequence'.json AS data, 'by-sequence'.doc_id, 'by-sequence'.rev", "FROM 'by-sequence' INDEXED BY ".concat(indexName, ", 'document-store'"), "WHERE 'by-sequence'.seq = 'document-store'.winningseq AND ".concat(whereClause)].join(' ');
 
   if (skip > 0) {
     sql += " OFFSET ".concat(skip);
@@ -265,7 +265,7 @@ var makeSQLQueryFromMango = function makeSQLQueryFromMango(_ref2) {
 exports.makeSQLQueryFromMango = makeSQLQueryFromMango;
 
 var makeSQLQueryForId = function makeSQLQueryForId(id) {
-  var sql = ["SELECT json AS data, doc_id, rev", "FROM 'by-sequence'", "WHERE doc_id=\"".concat(id, "\" AND deleted=0"), ";"].join(' ');
+  var sql = ["SELECT 'by-sequence'.json AS data, 'by-sequence'.doc_id, 'by-sequence'.rev", "FROM 'document-store', 'by-sequence'", "WHERE 'by-sequence'.seq = 'document-store'.winningseq AND 'document-store'.id=\"".concat(id, "\" AND 'by-sequence'.deleted=0"), ";"].join(' ');
   return sql;
 };
 
@@ -273,7 +273,7 @@ exports.makeSQLQueryForId = makeSQLQueryForId;
 
 var makeSQLQueryForIds = function makeSQLQueryForIds(ids) {
   var doc_ids = ids.join(',');
-  var sql = "\n    SELECT json AS data, doc_id, rev\n    FROM 'by-sequence'\n    WHERE doc_id IN (\"".concat(doc_ids, "\") AND deleted = 0;\n  ");
+  var sql = "\n    SELECT 'by-sequence'.json AS data, 'by-sequence'.doc_id, 'by-sequence'.rev\n    FROM 'document-store', 'by-sequence'\n    WHERE 'by-sequence'.seq = 'document-store'.winningseq AND 'document-store'.id IN (\"".concat(doc_ids, "\") AND 'by-sequence'.deleted = 0;\n  ");
   return sql;
 };
 
@@ -286,7 +286,7 @@ var makeSQLQueryAll = function makeSQLQueryAll() {
       _ref3$skip = _ref3.skip,
       skip = _ref3$skip === void 0 ? 0 : _ref3$skip;
 
-  var sql = ["SELECT json AS data, doc_id, rev", "FROM 'by-sequence'", "WHERE deleted=0", "LIMIT ".concat(limit)].join(' ');
+  var sql = ["SELECT 'by-sequence'.json AS data, 'by-sequence'.doc_id, 'by-sequence'.rev", "FROM 'document-store', 'by-sequence'", "WHERE 'by-sequence'.seq = 'document-store'.winningseq AND 'by-sequence'.deleted=0", "LIMIT ".concat(limit)].join(' ');
 
   if (skip > 0) {
     sql += " OFFSET ".concat(skip);
@@ -404,7 +404,13 @@ var executeSQL = /*#__PURE__*/function () {
       while (1) {
         switch (_context3.prev = _context3.next) {
           case 0:
-            return _context3.abrupt("return", db.executeAsync(sql));
+            return _context3.abrupt("return", db.executeAsync(sql).catch(function (err) {
+              var m = err && err.message || String(err);
+              if (/no such table|database is locked/i.test(m)) {
+                return { rows: { length: 0, item: function item() {} } };
+              }
+              throw err;
+            }));
 
           case 1:
           case "end":
diff --git a/node_modules/cozy-pouch-link/dist/db/sqlite/sqliteDb.native.js b/node_modules/cozy-pouch-link/dist/db/sqlite/sqliteDb.native.js
index 9a11cd1..742383d 100644
--- a/node_modules/cozy-pouch-link/dist/db/sqlite/sqliteDb.native.js
+++ b/node_modules/cozy-pouch-link/dist/db/sqlite/sqliteDb.native.js
@@ -57,19 +57,28 @@ var SQLiteQueryEngine = /*#__PURE__*/function (_DatabaseQueryEngine) {
     key: "openDB",
     value: function openDB(dbName) {
       var fileDbName = "".concat(dbName, ".sqlite");
-
-      try {
-        this.db = (0, _opSqlite.open)({
-          name: fileDbName
-        }); // Create index at db opening if needed
-
-        var docIdIndexSql = (0, _sql.makeSQLCreateDocIDIndex)();
-        var deletedIndexSql = (0, _sql.makeSQLCreateDeletedIndex)();
-        (0, _sql.executeSQL)(this.db, docIdIndexSql);
-        (0, _sql.executeSQL)(this.db, deletedIndexSql);
-      } catch (err) {
-        _logger.default.error(err);
-      }
+      var resolved = null;
+      Object.defineProperty(this, 'db', {
+        configurable: true,
+        set: function set(v) {
+          resolved = v;
+        },
+        get: function get() {
+          if (resolved) return resolved;
+          resolved = (0, _opSqlite.open)({
+            name: fileDbName
+          });
+          try {
+            (0, _sql.executeSQL)(resolved, "PRAGMA journal_mode=WAL");
+            (0, _sql.executeSQL)(resolved, "PRAGMA busy_timeout=5000");
+            (0, _sql.executeSQL)(resolved, "CREATE INDEX IF NOT EXISTS 'by_docid_and_deleted' ON 'by-sequence' (doc_id, deleted)");
+            (0, _sql.executeSQL)(resolved, (0, _sql.makeSQLCreateDeletedIndex)());
+          } catch (err) {
+            _logger.default.error(err);
+          }
+          return resolved;
+        }
+      });
     }
   }, {
     key: "allDocs",
diff --git a/node_modules/cozy-pouch-link/dist/jsonapi.js b/node_modules/cozy-pouch-link/dist/jsonapi.js
index 0d54cae..493620f 100644
--- a/node_modules/cozy-pouch-link/dist/jsonapi.js
+++ b/node_modules/cozy-pouch-link/dist/jsonapi.js
@@ -63,6 +63,14 @@ var resetAllPaths = function resetAllPaths() {
 exports.resetAllPaths = resetAllPaths;
 
 var normalizeDocs = function normalizeDocs(client, doctype, docs) {
+  if (doctype === 'io.cozy.files') {
+    for (var j = 0; j < docs.length; j++) {
+      var dir = docs[j];
+      if (dir && dir.type === _files.TYPE_DIRECTORY && dir.path) {
+        setFilePath(dir._id || dir.id, dir.path);
+      }
+    }
+  }
   for (var i = docs.length; i >= 0; i--) {
     var doc = docs[i];
 
