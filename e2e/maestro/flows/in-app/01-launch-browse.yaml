appId: com.linagora.twakedrive
tags:
  - inapp
env:
  TEST_FOLDER: 'LINAGORA'
---
# Cross-platform. Sur iOS le dossier a l'accessibilité "<nom>, folder actions" →
# le regex '${TEST_FOLDER}.*' matche iOS ET Android. Retour via le testID
# appbar-back-button (iOS n'a pas de bouton retour matériel).
- runFlow: ../../subflows/openDrive.yaml
- tapOn:
    text: '${TEST_FOLDER}.*'
- tapOn:
    id: 'appbar-back-button'
- runFlow: ../../subflows/assertLoggedIn.yaml
