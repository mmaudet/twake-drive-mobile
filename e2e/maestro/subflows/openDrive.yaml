appId: com.linagora.twakedrive
---
# Brings the app to the drive root, pre-auth session assumed active.
# launchApp cold-restarts → lands on the Mon Drive tab by default.
- launchApp
- runFlow: assertLoggedIn.yaml
