appId: com.linagora.twakedrive
---
# Fails fast and clearly if the pre-auth session is absent
# (login screen instead of the drive shell). The "Mon Drive" tab is
# only present in the authenticated shell.
- assertVisible: 'Mon Drive'
